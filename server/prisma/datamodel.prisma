type User {
  id: ID! @unique
  email: String! @unique
  password: String!
  companyId: String!
}

type Company {
  id: ID! @unique
  name: String!
  description: String!
}

type Job {
  id: ID! @unique,
  companyId: String!
  title: String!
  description: String!
}