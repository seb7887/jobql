type User {
  id: ID! @unique
  email: String! @unique
  password: String!
  company: Company!
}

type Company {
  id: ID! @unique
  name: String!
  description: String!
  jobs: [Job]
}

type Job {
  id: ID! @unique,
  company: Company!
  title: String!
  description: String!
}
